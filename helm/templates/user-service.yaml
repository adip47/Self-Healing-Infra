{{- $cfg := index .Values "user_service" -}}
{{- if $cfg.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: apps
  labels:
    app: user-service
spec:
  replicas: {{ $cfg.replicaCount }}
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
        - name: user-service
          image: "{{ $cfg.image.repository }}:{{ $cfg.image.tag }}"
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          resources:
            limits:
              cpu: {{ $cfg.resources.limits.cpu | quote }}
              memory: {{ $cfg.resources.limits.memory | quote }}
            requests:
              cpu: {{ $cfg.resources.requests.cpu | quote }}
              memory: {{ $cfg.resources.requests.memory | quote }}
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: apps
spec:
  type: {{ $cfg.service.type }}
  selector:
    app: user-service
  ports:
    - name: http
      port: {{ $cfg.service.port }}
      targetPort: 8080
---
{{- if $cfg.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: user-service-hpa
  namespace: apps
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: user-service
  minReplicas: {{ $cfg.hpa.minReplicas }}
  maxReplicas: {{ $cfg.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ $cfg.hpa.cpu.averageUtilization }}
{{- end }}
{{- end }}
